# Allure Configuration
allure.results.directory=${project.build.directory}/allure-results
allure.report.directory=${project.build.directory}/allure-report
allure.clean.on.start=true
allure.attachments.include_passed_attachments=true

# Disable automatic report generation (we'll handle it with Maven)
allure.report.generate=false

# Environment settings
allure.issues.tracker.pattern=
allure.tests.management.pattern=

# Logging settings
allure.logging.level=INFO
